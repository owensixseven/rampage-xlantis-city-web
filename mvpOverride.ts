// ─────────────────────────────────────────────────────────────────────────
// MANUALLY-MANAGED TOURNAMENT MVP
//
// By default, the Tournament MVP shown on the Home page and MVP page is
// whichever player has the highest computed mvpScore (demo data). To force
// a SPECIFIC player as MVP instead, set their exact name below (matched
// case-insensitively against the names in playerRoster.ts).
//
// Example:
//   export const MVP_OVERRIDE_PLAYER_NAME: string | null = 'NEELAKANDAN'
//
// Set it back to null to return to automatic (highest score) selection.
// ─────────────────────────────────────────────────────────────────────────

export const MVP_OVERRIDE_PLAYER_NAME: string | null = 'NEELAKANDAN'
